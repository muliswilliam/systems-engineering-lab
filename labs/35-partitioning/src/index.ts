import { createLogger } from "@labs/logging";

const log = createLogger("lab35");

log.info(
  "Lab 35 - partitioning. See README.md for setup and pnpm scenario:* scripts (query-comparison, no-benefit-query, partition-maintenance, attach-and-missing-partition, list-partitioning).",
);
